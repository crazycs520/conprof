scrape_configs:
- job_name: 'tidb'
  scrape_interval: 5s
  scrape_timeout: 10s
  profiling_config:
    pprof_config:
      profile:
        enabled: true
        seconds: 5
      goroutine:
        debug: 2
  static_configs:
  - targets: ['0.0.0.0:10080']
